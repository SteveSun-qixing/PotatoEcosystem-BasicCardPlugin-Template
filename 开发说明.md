# 基础卡片插件开发说明

## 🏗️ 生态架构说明

### monorepo 工作区

BasicCardPlugin 是薯片生态 **pnpm workspace** 的一部分，**不是独立的 npm 包**。

```
Project-12/ (主仓库)
├── pnpm-workspace.yaml          # 工作区配置
├── Chips-Foundation/            # 依赖：公共基础层
├── Chips-SDK/                   # 依赖：SDK层
├── Chips-ComponentLibrary/      # 依赖：组件库
└── BasicCardPlugin/
    ├── Rich-Text-Basic-Card-Plugin/    # 已有插件
    └── Your-Plugin/                    # 你的新插件 ⚠️
```

### 共用依赖

所有模块共享 node_modules：
- 在主仓库根目录运行 `pnpm install`
- 所有子模块的依赖统一安装
- 避免重复依赖，节省空间

---

## 📦 依赖配置

### package.json 配置

```json
{
  "name": "@chips/your-plugin",
  "version": "1.0.0",
  "peerDependencies": {
    "@chips/foundation": "workspace:*",   // ⚠️ 使用 workspace 协议
    "@chips/sdk": "workspace:*"
  }
}
```

**关键点**：
- ✅ 使用 `workspace:*` 引用工作区内的模块
- ❌ 不使用 `^1.0.0` 版本号
- ✅ `peerDependencies` 不是 `dependencies`

---

## 🚀 开发流程

### 1. 初始化

```bash
# 在主仓库根目录
cd /path/to/Project-12

# 复制模板
cp -r BasicCardPlugin/Basic-Card-Plugin-Template BasicCardPlugin/Your-Plugin

cd BasicCardPlugin/Your-Plugin

# 全局替换 Template → YourCard
# 修改 package.json 中的 name
```

### 2. 安装依赖

```bash
# 回到主仓库根目录
cd /path/to/Project-12

# 安装所有工作区依赖
pnpm install
```

### 3. 构建依赖模块

```bash
# 按顺序构建依赖的模块
cd Chips-Foundation && pnpm run build
cd ../Chips-SDK && pnpm run build
cd ../Chips-ComponentLibrary && pnpm run build
```

### 4. 开发插件

```bash
cd BasicCardPlugin/Your-Plugin

# 启动开发服务器
pnpm run dev

# 运行测试
pnpm test

# 构建
pnpm run build
```

---

## 📝 Git 仓库管理

### 作为 Submodule

如果你的插件需要独立的 Git 仓库（推荐）：

```bash
# 1. 在 GitHub 创建新仓库
# 例如：PotatoEcosystem-YourCard-Plugin

# 2. 在插件目录初始化 Git
cd BasicCardPlugin/Your-Plugin
git init
git add .
git commit -m "feat: 初始化插件"

# 3. 添加远程仓库
git remote add origin https://github.com/你的用户名/PotatoEcosystem-YourCard-Plugin.git
git push -u origin main
git checkout -b develop
git push -u origin develop

# 4. 回到主仓库，添加为 submodule
cd /path/to/Project-12
git submodule add https://github.com/你的用户名/PotatoEcosystem-YourCard-Plugin.git BasicCardPlugin/Your-Plugin

# 5. 更新 pnpm-workspace.yaml
# 添加：- 'BasicCardPlugin/Your-Plugin'

# 6. 提交到主仓库
git add .gitmodules pnpm-workspace.yaml BasicCardPlugin/Your-Plugin
git commit -m "chore: 添加 Your-Plugin 子模块"
git push
```

---

## 🔧 模块间引用

### TypeScript 导入

```typescript
// ✅ 正确：通过包名导入
import { ChipsSDK } from '@chips/sdk';
import { Button } from '@chips/components';
import type { ChipsCore } from '@chips/foundation';

// ❌ 错误：不使用相对路径跨模块
import { something } from '../../Chips-SDK/src/xxx';
```

### 依赖声明

```json
{
  "peerDependencies": {
    // 工作区内的模块
    "@chips/foundation": "workspace:*",
    "@chips/sdk": "workspace:*",
    "@chips/components": "workspace:*",
    
    // 外部依赖使用正常版本号
    "vue": "^3.4.0"
  }
}
```

---

## 📋 添加到工作区

### 1. 更新 pnpm-workspace.yaml

在主仓库的 `pnpm-workspace.yaml` 中添加：

```yaml
packages:
  # ... 其他包
  
  # 插件
  - 'BasicCardPlugin/Rich-Text-Basic-Card-Plugin'
  - 'BasicCardPlugin/Your-Plugin'              # ⚠️ 添加这行
```

### 2. 重新安装

```bash
cd /path/to/Project-12
pnpm install
```

### 3. 验证

```bash
# 检查是否被识别为工作区包
pnpm list --depth 0

# 应该看到 @chips/your-plugin
```

---

## ⚠️ 重要注意事项

### 1. 不发布到 npm

基础卡片插件：
- ❌ 不单独发布到 npm
- ✅ 在生态内作为工作区包使用
- ✅ 通过薯片应用市场分发

### 2. 依赖管理

- ✅ 在主仓库根目录运行 `pnpm install`
- ❌ 不在插件目录单独 `npm install`
- ✅ 使用 `workspace:*` 引用内部模块

### 3. 构建顺序

- ✅ 先构建依赖的模块（Foundation, SDK）
- ✅ 再构建插件
- ❌ 不能在依赖未构建时构建插件

### 4. Git 管理

- ✅ 作为 submodule 独立仓库（推荐）
- ✅ 或直接在主仓库中（简单项目）
- ✅ 在 pnpm-workspace.yaml 中注册

---

## 🔗 相关文档

- [仓库管理手册](../../生态共用/仓库管理手册.md)
- [依赖管理指南](../../生态共用/12-依赖管理指南.md)
- [开发环境配置指南](../../生态共用/15-开发环境配置指南.md)

---

**文档版本**: 1.0  
**最后更新**: 2026-02-04
